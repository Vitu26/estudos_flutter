import 'package:flutter/material.dart';
import 'myapp.dart';

void main() {
  runApp(MyApp());
}
//runApp que é o responsável por rodar o app
// O app é uma classa que está estendendo da classe StatelessWidget
//o metodo build é o resposável por desenhar na tela e todas as classes precisam ter ele
// o scafolld é nosso template(nossa página principal) do app

